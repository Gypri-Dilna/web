# Normalise line endings in the repository so the working tree stays consistent
# across Windows, macOS and Linux checkouts.
* text=auto eol=lf

# Binary assets — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.pdf binary

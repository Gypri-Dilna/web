# antislop audit 003, 2026-09-11

Mode: **After**. Scope: the whimsy pass, which raised the dials to
ENERGY 3 / RHYTHM 3 / MOTION 2 and added real photography.

The detector was run after the pass and flagged exactly two of my own additions.
Both are recorded here, with what I did about each.

---

## What the pass changed

Before this pass the site read as bland and corporate. The cause was assets, not CSS:
it had one screenshot, three generic tool pictograms, and thumbnails from an old
WordPress page. The workshop's own archive of 232 photographs was reviewed and
20 were selected. That, plus one warm colour, is the whole fix.

Added: real photography throughout, an orange accent taken from the Gypri whistles
the workshop prints, tilted photo frames, an eyebrow tick, a rule under each section
heading that draws itself in, a ruler-tick divider, a chip, and a turning logo.

Dials moved: ENERGY 2 to 3, RHYTHM 2 to 3, MOTION 1 to 2. All recorded in `DESIGN.md`.

---

## Findings from the detector

### 1. `marquee` (warning): FIXED by removal

I built an auto-scrolling band of the workshop's disciplines: 3D tisk, Robotika,
Elektronika, Pájení, Programování, CNC, Bastlení.

The detector's objection was right, and for a reason I should have caught before
building it: **the same four words are already listed in the section immediately
below it**. The band repeated existing content, so the motion carried no information.
That is the definition of attention that has not been earned. It also hid half its
own content at any moment, and it demanded a pause-on-hover affordance to be usable.

A marquee is also the single most recognisable "make it feel fun" move, which is
exactly the kind of default this whole process exists to catch. The genuine whimsy on
this site is the photography: students crowded around a PC build, an orange whistle in
the palm of a hand. That does not need a moving band next to it.

Removed: the markup, the CSS block, the keyframes, the reduced-motion rules, and the
type-scale row. Nothing replaced it.

### 2. `repeating-stripes-gradient` (advisory): FIXED by redrawing

The tick divider used `repeating-linear-gradient` to draw ruler ticks.

The rule asks for "a deliberate texture" rather than a gradient stripe, and it is
right that a repeating gradient is the generic way to get this look. But the ruler
itself is a real idea: a bench is ruled and measured, and this is the site's identity
motif after the hex-nut chain was cut for repeating the logo too literally.

So rather than delete the element, I redrew it as an explicit inline SVG tick, plus a
hairline and one orange measured mark. The intent is now in the code as a drawn tick
instead of a gradient, which is the deliberate version the rule asks for.

---

## Verified after the changes

- impeccable: **0 findings**
- all 15 text/surface colour pairs pass WCAG AA, including every orange use
  (orange label on page 7.02:1, chip dark-on-orange 6.40:1)
- 0 broken images, 0 overflow, at 1440px and 390px
- reduced motion: reveals forced visible rather than waiting on the observer,
  tilt neutralised, no self-running animation
- 0 em dashes; 74 CSS classes declared, all 74 used

## One bug this pass introduced and fixed

The whimsy rewrite replaced the block that held `.js .reveal`, so the reveal CSS
vanished while `main.js` still added `.is-visible`. Separately, the head script that
sets the `.js` class had been missing since the antislop pass, so the reveal rules were
inert. Both are fixed, and verification now asserts:
the `.js` class is set before `</head>`, `.js .reveal` exists, `main.js` observes both
`.reveal` and `.section-head`, and reduced motion forces reveals visible.

---

## Follow-up, same day: the second accent was wrong

A second accent was added in this pass, Whistle Orange `#ff6b35`, on the reasoning that
the workshop's orange products made it brand evidence. That was wrong on both counts and
it has been removed.

**Why it was wrong.** The brand guide names three colours: Graphite Core, Circuit Mint and
Cloud Paper. Orange is a *product* colour, not a brand colour. The antislop pass had
already established a one-accent rule for this site, and this document recorded it. Adding
a second accent silently overrode a decision that had been made deliberately, in response
to a request ("less bland") that was about liveliness, not about palette.

**What replaced it.** Every gesture that used orange now draws in Circuit Mint: the eyebrow
tick, the rule under each section heading, the key-phrase underline, the ruler mark, and
the callout label. `label-orange` was renamed `label-accent`. The hero scrim lost its warm
tint and is now plain dark, which also stops a colour cast fighting the yellow building in
the photograph. The `.whistle-note` callout lost its orange border and wash and is now a
neutral raised surface, and becomes the rule-consistent `.callout`.

**The underline was also rebuilt.** It was a solid colour bar painted behind the words,
which lowered the contrast of the glyphs sitting on it and could not wrap, so a long marked
phrase was clipped off-screen on mobile. It is now a `text-decoration` underline: it draws
below the baseline, so it never touches text contrast, and it breaks across lines like
normal text. Measured: `scrollWidth` on cansat.html at 390px went from 676 to 390.

## Two more bugs found while checking this, both real

**1. `.feature-item` squeezed its text into a 34px column.** The element was a two-column
grid (`34px 1fr`) from when each item carried a pictogram beside its text. The pictograms
were deleted in favour of real photographs, but the grid column stayed, so the copy was
rendered one word per line. Visible in a screenshot, invisible to every automated check I
had. Fixed by removing the grid, and the now-redundant wrapper `<div>`s came out of the
markup too.

**2. The earlier mobile check was too narrow.** I had verified mobile on `index.html` only.
The clipped underline was worst on `cansat.html`, whose marked phrase is far longer. The
lesson is in the verification now: all five pages are checked at 320, 390 and 768px.

## Final state

- impeccable: 0 findings
- design-guard: 5/5 pages at 100/100, `DESIGN.md` 73/100
- no orange in any shipping file; the docs record the removal rather than hiding it
- every mint-on-surface pair passes AA: label on page 6.52:1, label on callout 6.08:1,
  chip text 5.76:1, current-page marker on the top bar 6.52:1
- 0 horizontal scroll and 0 text overflow on all five pages at 320, 390 and 768px
- `.mark` is a wrapping underline, not a bar behind text

---

# Later pass: the instrumentation lane

The owner asked for the site to feel like Teenage Engineering or Nothing. Both companies
were researched from primary and secondary sources, and the design direction is recorded in
`DESIGN.md`. The short version: the page is now a datasheet for a real workshop.

## What changed, and what was deliberately refused

Adopted: a monospace face for measured values only, numbered figure plates, a real datasheet
component, radius 0, strictly aligned photographs, and no motion that runs on its own.

Refused, and this is the important half:

- **Their palettes.** Teenage Engineering's identity colour is orange; Nothing's accents are
  red, blue and yellow. The brand guidelines name three colours and only one is chromatic,
  so mint carries everything. Copying a reference brand's colour is not applying its
  philosophy, it is wearing its clothes. Re-adding orange here would also have reversed the
  correction made earlier in this same file.
- **Their typefaces.** Nothing ships NType82 and NDot55 under licence; TE ships its own.
  Adding a licensed face would have broken the brand guide's mandated typeface and added a
  download. The instrumentation voice is a system monospace stack instead: zero bytes, zero
  licence, nothing to break offline.
- **Replacing Montserrat.** The guide mandates it. It still sets every heading, paragraph,
  nav item and button. Only measured values changed face.

The owner chose the mono option explicitly, and chose to keep the playfulness while
straightening the photography. Both decisions are recorded in `DESIGN.md`.

## The photography

The tilted frames were removed. A one-degree rotation read as scrapbook rather than as
instrumentation, and neither reference brand tolerates a crooked alignment. Twenty-six
wrapper `<div>`s collapsed into the media frame they were wrapping, so the markup got
smaller, not larger. The play that the owner asked to keep now lives in the copy, in the
whistle section, and in one small interaction: hovering a figure turns its plate number
mint.

## Documentation

`DESIGN.md` gained a section explaining the lane, a table of what was adopted against what
was refused, and the sources consulted. It also records the two faces and the test that
decides between them: if a value could be read aloud as part of a sentence it stays in
Montserrat, and if it is a reading off an instrument it goes in mono.

## Verification

- impeccable: 0 findings
- design-guard: 5/5 pages at 100/100, `DESIGN.md` **85/100** (up from 73)
- axe-core WCAG 2.1 A/AA: 0 violations on all five pages
- 0 horizontal scroll, 0 overflowing elements, 0 text overflow on all five pages at
  320, 390 and 768px
- all 26 figure captions carry a plate number; both datasheets are `<dl>` with real
  `<dt>`/`<dd>` pairing
- exactly one font download remains (Montserrat); `--mono` is a system stack

### A design-guard false positive worth recording

`scoreActionability` in `@design-guard/core` scans the typography section for any
`| word | value |` table cell pair and treats the second cell as a font size, deducting 2
when it does not match `px|rem`. It therefore reads a **line-height** column as a size and
penalises it. The old four-column table collected 6 such false positives (Actionability
13/25); adding a face column took it to 13 and dropped the score to 60 (Actionability 0/25).

This is a bug in the detector, not a defect in the document, and it is provable: the sizes in
that table were already all rem or clamp values. The table was restructured to
`Element | Size | Treatment`, which is clearer to read and happens to sidestep the
pairing (Actionability 25/25). No information was dropped; weight, leading and face moved
into the treatment column.

Recorded here rather than silently patched, because a future pass adding a column should
know why the table has three.

One earlier axe-core failure was likewise a false positive, and is recorded because it will
happen again to anyone auditing this site. Auditing a page while the reveal transition is
mid-flight samples a partially transparent element and reports contrast as low as 1.36:1.
Waiting for the transition to settle gives 0 violations on the same page. Audit after the
reveal, not during it.

---

# Later pass: the story

The owner's note was that the site "needs to have a better idea and a thought", that it had
to "feel like a story that you want to read", and that it should be "inviting, a bit strange
and unknown, like something you've never seen before". They supplied one link and asked for
research on what makes a site welcoming.

## The diagnosis

They were right, and the problem was structural rather than visual. The page was a
well-executed brochure: hero, what we do, proof, equipment, contact. Every section answered
a question the visitor had not asked, so nothing gave them a reason to read past the first
screen. All the craft was in place and none of it was doing any work, because there was no
withheld information anywhere on the page.

## The research, and what was taken from it

Loewenstein's information gap theory, via a chapter on the curiosity gap, supplied the two
rules the page is now built on. First, a gap only itches if the reader has enough context to
notice what is missing. Second, curiosity and clickbait are the same technique and differ
only in whether the promise is kept.

A scrollytelling design reference was read and mostly declined. Its machinery is sticky
graphic panels, parallax, scroll-linked animation, progress bars and scroll cues. All of it
is JavaScript, all of it needs a reduced-motion fallback, and none of it was necessary: the
same pacing comes from the order of the sections and the space each beat is given. One
principle was adopted, "show then tell", which is why the photograph now follows the
question rather than preceding it.

The supplied listicle was read in full and used for nothing. Its advice is a clear value
proposition, white space, subtle animation and social proof, which is the median technology
site, and which is the specific thing the owner called bland. This is recorded so a later
pass does not mistake it for a reference.

## The idea, and what it changed

The site is now a walk into a real room. There is a real door numbered 64 in the basement of
a real school, and the room behind it manufactures things the school quietly depends on,
including the evacuation whistles behind safety glass in every classroom.

That gave the homepage an arc: arrive at the door, see the building, go down a floor, look
around the room, and find out the room is not a hobby. Two things follow and are both
checked in the build verification. The gap opens before the payoff and with enough context
to itch. Every promise is kept, because the door number, the floor, the whistles and the
satellite are all real.

The reframing also fixed a duplication that had been there for several passes: the same
drone photograph was running full bleed on three pages at once, which made the one image the
site is built on read as a template background. It is now a numbered plate that appears
exactly once.

## The unfamiliar moves

- **Type before photographs.** The first viewport is a dark field with a title on it. Almost
  every other site opens with a large image.
- **A door number set in the instrumentation face.** "Dveře" in Montserrat beside "64" in
  monospace at display size: a human sign and a machine number on one line.
- **A depth datum instead of a section number.** `±0` and `−1` are the two floors this
  building really has. It is a place marker and it is used exactly twice, so it never
  becomes decorative counting, which is what numbered section labels were removed for.
- **An inventory that reads like labels on drawers.** Large object names, one terse factual
  line each, a real count where a count exists.
- **Withholding.** The room is described before it is explained.

## Cleanup this pass forced

Reordering the homepage orphaned a lot of component CSS: `.feature-split`, `.feature-lead`,
`.feature-rest`, `.feature-item`, the `.listlinks` family, the `.data` family, `.hero-photo`
and `.gallery`. All of it was deleted, taking the stylesheet from 84 declared class
selectors to 75. The dead-CSS check in the build verification now fails if any of them
returns without markup to match, so the stylesheet and `DESIGN.md` cannot drift apart again.

One finding from impeccable was legitimate and was fixed rather than waived. The story's
statement line wrapped to two lines with `line-height: 1.08`, which is correct for a
two-word headline and wrong for a full sentence. It is now 1.3. A `.statement + .lede` rule
was also written for a class that never appeared in the markup, and was deleted.

## Verification

- impeccable: 0 findings
- design-guard: 5/5 pages at 100/100, `DESIGN.md` 85/100
- axe-core WCAG 2.1 A/AA: 0 violations on all five pages, audited after the reveal settles
- 0 horizontal scroll, 0 overflowing elements, 0 text overflow on all five pages at
  320, 390, 768 and 1440px
- 84 declared class selectors reduced to 75, 0 dead beyond the JS gate class
- the arrival photograph appears once; the depth datum appears exactly twice

---

## Follow-up, 2026-09-13: re-verified before commit, and two stale lines fixed

The pass was re-run against a clean tree rather than committed on the strength of the
write-up above.

- impeccable: 0 findings. design-guard: 5/5 pages at 100/100, `DESIGN.md` 85/100.
- axe-core WCAG 2.1 A/AA: 0 violations on all five pages at 390px. The contrast false
  positive described earlier still appears if the audit runs before the reveal transition
  settles; the blended foregrounds it reports (`#3b3e47`, `#15171c`, `#232427`) are the
  giveaway. Waiting for the fade to finish gives 0.
- 0 horizontal overflow at 390 and 1440px on every page.
- 0 dead class selectors. Counted by stripping comments and `url()` from `styles.css`,
  extracting every class, and matching each against the markup and `main.js`. (The 75 above
  came from a looser count that included selector variants; the number that matters is the
  zero.)
- All 27 image references resolve and no image file is unused: 27 files, 27 references.

Two lines in `DESIGN.md` still described the design as it was two passes earlier. The
document is the source of truth, so a contradiction inside it is a defect, not cosmetics:

- **"Why a 2px radius?"** survived the pass that took radius to `0`, and sat three
  paragraphs below a table row saying the same. Now "Why square corners?", with the current
  reason.
- **"Why the dials went up"** still listed tilted photographs as a current ingredient. The
  tilt was removed in the instrumentation pass, so the line now records it as history.

One asset was deleted with the code: `photo/tiskarna-spulka.webp` lost its only `<img>` when
the homepage was reordered, so it was removed rather than left as an orphan. The folder is
20 photographs and 7 sponsor logos, which is what the README says it is.

---

# Later pass, 2026-09-16: the issue list

Ten issues were filed against the repository and worked through. Most were the owner
reading the site the way a visitor does, which is the reading that matters. Three were
gestures that had stopped doing a job, and two were real defects.

## Removed, because they did not read as intended

- **The mint mark on the tick divider.** It sat directly under whatever came before it and
  read as a stray coloured bar rather than as a measured mark, which is exactly how it was
  reported ("I don't understand the bar under the photo"). The divider is now a hairline and
  its ticks and nothing else. The ruler idea survives; the accent on it did not.
- **The number beside each inventory row.** `07` next to "Sedm tiskáren Prusa" repeated the
  word the name already contained, and only two of the six rows had one, so it read as an
  accident rather than as a datasheet. The counts stay in the names, where they read as
  sentences.
- **The logo turn on hover.** A moving part with no job: it told the visitor nothing, and it
  was the only thing in the chrome that moved. It also existed twice in the stylesheet, at
  two different angles, which is how a rule like that survives a cleanup.

## Fixed

- **The favicon was not the logo.** It was a mint hex nut on a solid dark rounded tile, so
  the tab showed a dark square and the nut, not the mark. It is now the actual logo glyph,
  in mint, on a transparent background so it reads on both light and dark chrome. The two
  halves of the interlocking mark meet along a hairline gap that is invisible when they are
  different colours; drawn in one colour it showed as a seam, so the glyph carries a
  same-colour hairline stroke to close it.
- **The mobile menu ghosted the page behind it.** The panel was `rgba(8, 9, 12, 0.97)` and
  `position: fixed`, so the hero ("DVEŘE 64" and the lead) showed through the open menu.
  That was the "glitch in the top bar". The panel is opaque now, and the links arrive one
  after another on a short transform and opacity stagger.
- **The page wasted the width of a large display.** `--maxw` was 1360px, so on a 2560px
  screen the whole composition sat in the middle 53% between two dead margins. Above 1600px
  the container is 1720px, and the root font-size grows from 16px toward 18.5px so the type
  scales with the wider column instead of floating in it. Prose keeps a 76ch measure, so
  widening the container did not turn paragraphs into 2000px lines. Measured after the
  change: at 2560px the container is 1720px (67%) with a 18.5px root and a 167px display
  title, against 1360px (53%), 16px and 144px before. Everything below 1600px is untouched.

## Content corrected

- **"Sítotisk loga".** Two figure captions and one datasheet row claimed the workshop
  screen-prints the logo on the whistles. It does not, yet. The captions now read "detail
  loga" and the row is gone rather than replaced with a guess; if the real method is named
  later the row can come back with it.
- **"Co pro CanSat vyrábíme"** linked to a page with no CanSat content at all, and that
  page's lead claimed the workshop built "a satellite". It builds parts for one. There is
  now a "Díly pro CanSat" section on `nase-prace.html`, the lead says so, and the CanSat
  button links to the section rather than the top of the page.
- **Three photographs in the "Naše práce" strip** were an empty room, a staged portrait with
  a hammer inside an empty shelving frame, and a PC build whose `alt` claimed it was
  soldering. They were replaced with photographs of actual work from the same archive, and
  the wrong `alt` on the PC build was fixed on the homepage too. The staged portrait is now
  unused and was deleted with its CSS-free self, which is the same call as the last pass.
  The folder is 19 photographs and 7 sponsor logos.

## Verification

- impeccable: 0 findings
- design-guard: 5/5 pages at 100/100, `DESIGN.md` 85/100
- axe-core WCAG 2.1 A/AA: 0 violations on all five pages, audited after the reveal settles
- 0 horizontal overflow at 320, 390, 768, 1440, 1920 and 2560px
- 0 dead class selectors (68 declared, all used)
- no broken images on any page; the sponsor hover, the menu, the favicon and the new CanSat
  section were all confirmed by eye in a browser, not by measurement alone
